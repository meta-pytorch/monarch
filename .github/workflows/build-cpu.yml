name: Build CPU

on:
  workflow_call:
    inputs:
      artifact-name:
        description: 'Wheel artifact name to upload'
        type: string
        default: 'monarch-cpu-py3.10'
      python-version:
        description: 'Python version to build with'
        type: string
        default: '3.10'
      runner:
        description: 'Linux runner to use for the build'
        type: string
        default: 'linux.4xlarge'

concurrency:
  group: build-cpu-${{ inputs.runner }}-${{ github.workflow }}-${{ github.ref == 'refs/heads/main' && github.run_number || github.ref }}
  cancel-in-progress: true

jobs:
  build-cpu:
    name: Build CPU - Tensor Engine
    uses: pytorch/test-infra/.github/workflows/linux_job_v2.yml@main
    with:
      timeout: 60
      runner: ${{ inputs.runner }}
      submodules: recursive
      upload-artifact: ${{ inputs.artifact-name }}
      script: |
        # Source common setup functions
        source scripts/common-setup.sh

        # Setup build environment (conda + system deps + rust + build deps)
        setup_build_environment ${{ inputs.python-version }}

        # Install tensor engine C++ header deps (fmt for pytorch headers).
        # RDMA libs installed by this helper are harmless and unused when
        # MONARCH_GPU_PLATFORM=none gates the GPU layer off.
        setup_tensor_engine

        # Install torch nightly (CPU version)
        pip install --pre torch --extra-index-url https://download.pytorch.org/whl/nightly/cpu
        pip install -r build-requirements.txt

        # Build monarch with the CPU tensor engine (no CUDA/ROCm/RDMA).
        MONARCH_GPU_PLATFORM=none python setup.py bdist_wheel

        echo "=== sccache stats ==="
        sccache --show-stats || true
        echo "=== end sccache stats ==="
